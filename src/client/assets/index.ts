import { loadImage } from "../game/util";
import background from "./background.webp";
import ball from "./ball.svg";

export const backgroundImg = loadImage(background);
export const ballImg = loadImage(ball);
