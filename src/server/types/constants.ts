/* eslint-disable no-unused-vars */

export enum Host {
	dev = "localhost",
	prod = "?"
}

export enum Env {
	dev = "development",
	prod = "production"
}

export enum Routes {
	root = "/",
	helloWorld = "/helloworld",
	user = "/user"
}
